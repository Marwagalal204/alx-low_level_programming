#include "main.h"
/**
 * print_most_numbers - Entery point
 *
 * @i: integer
*/

void print_most_numbers(void);
{
	int i;

	for (i = ; i <= 9; i++)
	{
		if (i != 4 && i != 2)
		_putchar(i + '0')
	}
}		
